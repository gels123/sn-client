local speed = 10
local lightCpnt = nil

function start()
    print("lua start...")
    print("injected object", lightObject)
    lightCpnt= lightObject:GetComponent(typeof(CS.UnityEngine.Light))
    print("===call CS.MySta:ff1===", CS.MySta.ff1(100, 200)) --调用静态方法, 使用.
    
    local father  = CS.Father("fname", 33)
    father:Say() --调用静态方法, 使用:
    local son  = CS.Son("sname", 3)
    son:Say()
end

function update()
    local r = CS.UnityEngine.Vector3.up * CS.UnityEngine.Time.deltaTime * speed
    self.transform:Rotate(r)
    lightCpnt.color = CS.UnityEngine.Color(CS.UnityEngine.Mathf.Sin(CS.UnityEngine.Time.time) / 2 + 0.5, 0, 0, 1)
end

function ondestroy()
    print("lua destroy")
end

